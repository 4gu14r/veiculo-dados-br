$kind: http-request
description: Returns the details of a specific model year identified by `ano_id`, including the year value and the model it is associated with.
url: "{{baseUrl}}/api/v1/anos/:ano_id"
method: GET
headers:
  Accept: application/json
pathVariables:
  ano_id: "657"
examples: ./.resources/Detalhe de um ano.resources/examples
order: 2000
