$kind: http-request
description: Returns the full details of a specific vehicle model identified by `modelo_id`, including its name and the brand it belongs to.
url: "{{baseUrl}}/api/v1/modelos/:modelo_id"
method: GET
headers:
  Accept: application/json
pathVariables:
  modelo_id: "2774"
examples: ./.resources/Detalhe de um modelo.resources/examples
order: 2000
